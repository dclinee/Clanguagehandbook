#include <stdio.h>
#include <math.h>

#define PI 3.14159165

int main()
{

    double x, ret, val;
    x = 0.9;
    val = 180.0 / val;
    ret = asin(x) * val;

    printf("%lf is %lf", x, ret);
    return 0;
}
